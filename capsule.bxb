capsule {
  id (example.fact)
  version (1.0.0)
  format(3)
  targets {
    target (bixby-mobile-en-US)
  }
  capsule-imports {
    import (viv.core) {
      as (core)
    }
    import (viv.image) {
      as (image) version (2.11.4)
    }
  }
}

